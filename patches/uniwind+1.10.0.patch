diff --git a/node_modules/uniwind/dist/metro/transformer.cjs b/node_modules/uniwind/dist/metro/transformer.cjs
index b5da026..d03a86b 100644
--- a/node_modules/uniwind/dist/metro/transformer.cjs
+++ b/node_modules/uniwind/dist/metro/transformer.cjs
@@ -629,6 +629,19 @@ class CSS {
           return declarationValue.value;
         case "baseline-position":
           return "baseline";
+        case "filters": {
+          const filters = declarationValue.value.map((filter) => {
+            if (filter.type === "blur") {
+              const length = filter.value;
+              if (length.type === "value") {
+                return `blur(${length.value.value}${length.value.unit})`;
+              }
+              return void 0;
+            }
+            return void 0;
+          }).filter(config.isDefined);
+          return filters.length > 0 ? filters.join(" ") : void 0;
+        }
         default:
           if (Object.keys(declarationValue).length === 1) {
             return declarationValue.type;
@@ -931,8 +944,19 @@ class Functions {
       const color = `${fn.name}(${this.Processor.CSS.processValue(fn.arguments)})`;
       return this.Processor.Color.processColor(color);
     }
+    if (fn.name === "blur") {
+      const arg = fn.arguments[0];
+      let radius;
+      if (arg?.type === "dashed-ident" || arg?.type === "var") {
+        const name = arg.type === "var" ? arg.value.name.ident : arg.value;
+        const themeValue = this.Processor.vars[name];
+        radius = typeof themeValue === "number" ? `"${themeValue}px"` : `vars[${JSON.stringify(name)}]?.(vars)`;
+      } else {
+        radius = this.Processor.CSS.processValue(fn.arguments);
+      }
+      return `"blur(" + (${radius}) + ")"`;
+    }
     if ([
-      "blur",
       "brightness",
       "contrast",
       "grayscale",
diff --git a/node_modules/uniwind/dist/metro/transformer.mjs b/node_modules/uniwind/dist/metro/transformer.mjs
index 9be05ef..ab77df0 100644
--- a/node_modules/uniwind/dist/metro/transformer.mjs
+++ b/node_modules/uniwind/dist/metro/transformer.mjs
@@ -622,6 +622,19 @@ class CSS {
           return declarationValue.value;
         case "baseline-position":
           return "baseline";
+        case "filters": {
+          const filters = declarationValue.value.map((filter) => {
+            if (filter.type === "blur") {
+              const length = filter.value;
+              if (length.type === "value") {
+                return `blur(${length.value.value}${length.value.unit})`;
+              }
+              return void 0;
+            }
+            return void 0;
+          }).filter(isDefined);
+          return filters.length > 0 ? filters.join(" ") : void 0;
+        }
         default:
           if (Object.keys(declarationValue).length === 1) {
             return declarationValue.type;
@@ -924,8 +937,19 @@ class Functions {
       const color = `${fn.name}(${this.Processor.CSS.processValue(fn.arguments)})`;
       return this.Processor.Color.processColor(color);
     }
+    if (fn.name === "blur") {
+      const arg = fn.arguments[0];
+      let radius;
+      if (arg?.type === "dashed-ident" || arg?.type === "var") {
+        const name = arg.type === "var" ? arg.value.name.ident : arg.value;
+        const themeValue = this.Processor.vars[name];
+        radius = typeof themeValue === "number" ? `"${themeValue}px"` : `vars[${JSON.stringify(name)}]?.(vars)`;
+      } else {
+        radius = this.Processor.CSS.processValue(fn.arguments);
+      }
+      return `"blur(" + (${radius}) + ")"`;
+    }
     if ([
-      "blur",
       "brightness",
       "contrast",
       "grayscale",
diff --git a/node_modules/uniwind/src/bundler/css-processor/css.ts b/node_modules/uniwind/src/bundler/css-processor/css.ts
index 80e14c6..aaae466 100644
--- a/node_modules/uniwind/src/bundler/css-processor/css.ts
+++ b/node_modules/uniwind/src/bundler/css-processor/css.ts
@@ -288,6 +288,25 @@ export class CSS {
                     return declarationValue.value
                 case 'baseline-position':
                     return 'baseline'
+                case 'filters': {
+                    const filters = declarationValue.value
+                        .map(filter => {
+                            if (filter.type === 'blur') {
+                                const length = filter.value
+
+                                if (length.type === 'value') {
+                                    return `blur(${length.value.value}${length.value.unit})`
+                                }
+
+                                return undefined
+                            }
+
+                            return undefined
+                        })
+                        .filter(isDefined)
+
+                    return filters.length > 0 ? filters.join(' ') : undefined
+                }
                 default:
                     // CSS string properties like absolute, relative, italic, etc.
                     if (Object.keys(declarationValue).length === 1) {
diff --git a/node_modules/uniwind/src/bundler/css-processor/functions.ts b/node_modules/uniwind/src/bundler/css-processor/functions.ts
index 7c5d4bd..991bfac 100644
--- a/node_modules/uniwind/src/bundler/css-processor/functions.ts
+++ b/node_modules/uniwind/src/bundler/css-processor/functions.ts
@@ -86,9 +86,26 @@ export class Functions {
             return this.Processor.Color.processColor(color as CssColor)
         }
 
+        if (fn.name === 'blur') {
+            const arg = fn.arguments[0]
+            let radius
+
+            if (arg?.type === 'dashed-ident' || arg?.type === 'var') {
+                const name = arg.type === 'var' ? arg.value.name.ident : arg.value
+                const themeValue = this.Processor.vars[name]
+
+                radius = typeof themeValue === 'number'
+                    ? `"${themeValue}px"`
+                    : `vars[${JSON.stringify(name)}]?.(vars)`
+            } else {
+                radius = this.Processor.CSS.processValue(fn.arguments)
+            }
+
+            return `"blur(" + (${radius}) + ")"`
+        }
+
         if (
             [
-                'blur',
                 'brightness',
                 'contrast',
                 'grayscale',
